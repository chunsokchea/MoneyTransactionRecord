@model IEnumerable<MoneyTransferWeb.Models.ReturnTransaction>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.RAmountR)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.RAmountS)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Paid)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ReturnDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Transaction)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.RAmountR)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.RAmountS)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Paid)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ReturnDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Transaction.TransactionID)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ReturnID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ReturnID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ReturnID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
