@model IEnumerable<MoneyTransferWeb.Models.Debt>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DAmountR)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DAmountS)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DebtOwe)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Transaction)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DAmountR)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DAmountS)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DebtOwe)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Transaction.TransactionID)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.DebtID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.DebtID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.DebtID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
